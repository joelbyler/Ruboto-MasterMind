require 'spec_helper'

describe GuessResult do
  it "knows which numbers in a guess are correct" do
    #guess = GuessResult.new (4, 2, 1, 9)

  end

  it "knows which positions in a guess are correct" do
    # TODO
  end
end